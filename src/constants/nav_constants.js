/**
* Navigation constants
*/
const NAV = Object.freeze({
    HOME: {
        path: "/"
    },
    SUMMONER_PAGE: {
        path: "/summoner",
    },
    SUMMONER_NAME: {
        path: "/summoner/:summonerName"
    }
});

module.exports = {
    NAV
}